# db-vault
DB Vault: an internal, self-contained DB access-broker panel (Horizon/Filament-style) that mounts inside any Laravel 12 app at a configurable domain/path, with its own auth, 2FA, SPA, and JSON API.
